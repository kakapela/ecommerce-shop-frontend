import { NavHighlightDirective } from './nav-highlight.directive';

describe('NavHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new NavHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
